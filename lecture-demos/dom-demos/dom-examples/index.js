// let's use getElementById to get our element
let firstParagraphElement = document.getElementById("first-paragraph");
console.log(firstParagraphElement);

// let's use a CSS class selector to get our element
let secondParagraphElement = document.querySelector(".css-class");
console.log(secondParagraphElement);

// let's change the text of our firstParagraph
// firstParagraphElement.innerText += " we added this text";

// what if we wanted to add in some HTML
// firstParagraphElement.innerHTML += " even more <b>bolded text</b>";